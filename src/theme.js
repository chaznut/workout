import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#FF4500',
      main: '#FF4500',
      dark: '#FF4500',
      contrastText: '#fff'
    }
  }
});

export default theme;
